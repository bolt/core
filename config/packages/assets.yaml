framework:
  assets:
    json_manifest_path: '%kernel.project_dir%/%bolt.public_folder%/assets/manifest.json'
    packages:
      bolt:
        base_path: '/'
      public:
        base_path: '/'
      files:
        base_path: '/files/'
