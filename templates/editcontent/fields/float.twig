{% extends '@bolt/editcontent/fields/_base.twig' %}

{% block field %}

  {% if step is not defined %}
      {% set step = define.step|default(1) %}
  {% endif %}

  <editor-number 
    name="{{ name }}"
    :value="{{ value }}"
    :step="{{ step }}"
  ></editor-number>

{% endblock %}