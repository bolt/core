{% extends '@bolt/editcontent/fields/_base.twig' %}

{% block field %}
    <editor-text
        :value="{{ value|json_encode() }}"
        :label="'{{ label }}'"
        :name="'{{ name }}'"
        :type="'{{ class }}'"
    ></editor-text>
{% endblock %}
