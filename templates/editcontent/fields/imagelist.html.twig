{% extends '@bolt/editcontent/fields/_base.html.twig' %}

{% block field %}
    <input name="{{ name }}" placeholder="…" type="text" value="{{ value }}" class="form-control {{ class }}" {{ attributes|default()|raw }}>
    <input name="{{ name }}" placeholder="…" type="text" value="{{ value }}" class="form-control {{ class }}" {{ attributes|default()|raw }}>
    <input name="{{ name }}" placeholder="…" type="text" value="{{ value }}" class="form-control {{ class }}" {{ attributes|default()|raw }}>

    <mark>Note: This field is practically the same as the <code>filelist</code>-field.</mark>

{% endblock %}
