{% extends '@bolt/_partials/fields/_base.html.twig' %}

{% block field %}
    <input class="form-control {{ class }}" name="{{ name }}" placeholder="{{ placeholder }}"
           type="text" value="{{ value|join(", ") }}" {{ attributes|default()|raw }}>
{% endblock %}
