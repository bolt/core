{% extends '@bolt/_partials/fields/_base.html.twig' %}

{% block field %}
    {% for file in value %}
        <input name="{{ name }}" placeholder="…" type="text" value="{{ file }}" class="form-control {{ class }}" {{ attributes|default()|raw }}>
    {% endfor %}

    <mark>Note: This field is practically the same as the <code>imagelist</code>-field.</mark>

{% endblock %}
