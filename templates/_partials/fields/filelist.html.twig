{% extends '@bolt/_partials/fields/_base.html.twig' %}

{% block field %}
    {% for file in value %}
        <input class="form-control {{ class }}" name="{{ name }}" placeholder="…" type="text" value="{{ file }}"
            {{ attributes|default()|raw }}>
    {% endfor %}

    <mark>{{ 'filelist.remark'|trans|raw }}</mark>
{% endblock %}
