{% extends '@bolt/_partials/fields/_base.html.twig' %}

{% block field %}
        <field-email
            :id="'{{ id }}'"
            :name="'{{ name }}'"
            :value="{{ value|json_encode() }}"
        ></field-email>
{% endblock %}