{% extends '@bolt/_partials/fields/_base.html.twig' %}

{% block field %}
    {% for image in value %}
    <input class="form-control {{ class }}" name="{{ name }}" placeholder="…" type="text" value="{{ image }}"
           {{ attributes|default()|raw }}>
    {% endfor %}
    <mark>{{ 'imagelist.remark'|trans|raw }}</mark>

{% endblock %}
