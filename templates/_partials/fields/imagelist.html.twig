{% extends '@bolt/_partials/fields/_base.html.twig' %}

{% block field %}
    {% for image in value %}
    <input name="{{ name }}" placeholder="…" type="text" value="{{ image }}" class="form-control {{ class }}" {{ attributes|default()|raw }}>
    {% endfor %}
    <mark>{{ 'imagelist.remark'|trans|raw }}</mark>

{% endblock %}
