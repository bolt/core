# Adding the new services for Bolt 4.2.0
file: packages/doctrine_migrations.yaml
since: 4.2.0

add:
    doctrine_migrations:
        migrations_paths:
            'Bolt\DoctrineMigrations': '%kernel.project_dir%/vendor/bolt/core/migrations'
