# Remove two keys (and underlying values) from the file: `role_hierarchy`, `access_control`

file: packages/security.yaml
since: 4.3.0

remove:
    security:
        role_hierarchy: ~
        access_control: ~
